extends layout.pug

block content
    h1.error-text-center=`${error.message}`
    img.panda-img(src="/images/panda_error.jpg") 
    br
    p.error-text-center See below for the scary details.
    h2=`Status: ${error.status}`
        br
        p=`Stack: ${error.stack}`